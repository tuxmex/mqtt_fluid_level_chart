name: mqtt_fluid_level_chart
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  mqtt_client: ^9.6.1
  syncfusion_flutter_charts: ^20.2.46 # Añade esta línea para usar los gráficos de Syncfusion

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
